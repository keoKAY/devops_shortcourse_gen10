## NOTE for justfile 

```bash 
sudo snap install just --classic 
which just 
```


1. Create a file `Justfile`

```bash 
# to list all available recipes 
just --list 
just setup-infra # setup-infra is a recipe 
```