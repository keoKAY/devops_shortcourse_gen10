apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spring-nfs-pvc-external
spec:
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-client
  #volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
